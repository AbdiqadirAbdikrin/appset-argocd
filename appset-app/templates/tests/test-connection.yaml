apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appset-app.fullname" . }}-test-connection"
  labels:
    {{- include "appset-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appset-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
