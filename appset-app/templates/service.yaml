apiVersion: v1
kind: Service
metadata:
  name: {{ include "appset-app.fullname" . }}
  labels:
    {{- include "appset-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appset-app.selectorLabels" . | nindent 4 }}
